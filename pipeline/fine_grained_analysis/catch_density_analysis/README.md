# Catch-density analysis — used to spot "zero-catch" or "empty haul" cycles.

Idea: during core window, a very flat motion curve + low edge density +
normal brightness may indicate the net came up empty.

Status: stub — quick heuristic to implement after priorities 1-2.